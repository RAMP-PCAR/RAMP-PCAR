language: node_js

node_js:
  - "0.10"

branches:
  only:
    - develop
    - master
    - /^test/.*$/

before_install: 
  - npm install -g grunt-cli
  - npm install -g bower

script:
  - grunt

after_success: 
  - grunt gh-pages:travis